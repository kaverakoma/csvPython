class ConexaoModel:

    def __init__(self, drive, host, port, database, user, password):
        self.drive = drive
        self.host = host
        self.port = port
        self.database = database
        self.user = user
        self.password = password